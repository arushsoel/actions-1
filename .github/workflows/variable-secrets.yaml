name: Exploring variables and secrets
on: 
  schedule:
    - cron: "*/1 * * * *"

jobs:
  Docker:
    runs-on: ubuntu-latest
    steps:
    - name: Docker build
      run: docker build -t docker.io/dockerusername/imagename:latest
    
    - name: Docker login
      run: docker login --username=dockerusername --password=s3c
